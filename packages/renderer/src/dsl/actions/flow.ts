/**
 * flow Action 类型 —— 现为 Contract 单一真相源的 re-export（Issue #16 / M0-1）。
 * 本地重复定义已删除；字段语义说明见 @lowcode-platform/schema-contract。
 */
export type { IfAction, LoopAction } from '@lowcode-platform/schema-contract';
