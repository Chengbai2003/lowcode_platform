{
  "name": "lowcode-platform",
  "version": "1.0.0",
  "private": true,
  "description": "A low-code platform with JSON schema to code generation",
  "scripts": {
    "dev": "pnpm --filter @lowcode-platform/playground dev",
    "build": "pnpm -r build",
    "build:packages": "pnpm --filter './packages/**' build",
    "clean": "pnpm -r clean",
    "type-check": "pnpm -r type-check",
    "lint": "pnpm -r lint"
  },
  "keywords": ["lowcode", "low-code", "react", "typescript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/react": "^18.2.45",
    "@types/react-dom": "^18.2.18",
    "typescript": "^5.3.3",
    "vite": "^5.0.8"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
